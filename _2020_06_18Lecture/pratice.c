# include <stdio.h>

struct Student
{
	char name[20];
	int age;
	char address[30];
	float height;
	float weight;
};

void showStudent(strict Student st)
{
	printf("이름 : %s\n", st.name);
	printf("나이")
}